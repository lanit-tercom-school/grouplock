comment:
  layout: header, changes, diff, tree, sunburst
coverage:
  precision: 0
  round: down
  range: "30...100"
  ignore:
  - GroupLockiOS/GroupLockTests/*
  status:
    patch:
      default:
        target: auto
    project:
      default:
        target: auto
